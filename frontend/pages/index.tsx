import React from 'react'
import App from '../client/src/App'

export default function IndexPage() {
  // Render the existing App component as the Next.js index page so the
  // application looks identical after migration.
  return <App />
}
